worker_processes 2;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    
    # Gzip compression
    gzip on;
    gzip_types text/plain application/json application/javascript text/css application/xml;
    
    # WebSocket upgrade handling
    map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
    }
    
    # Upstream servers
    upstream rpc_backend {
        server 127.0.0.1:26657;
    }
    
    upstream api_backend {
        server 127.0.0.1:1317;
    }
    
    upstream grpc_backend {
        server 127.0.0.1:9090;
    }
    
    upstream ws_p2p_backend {
        server 127.0.0.1:8081;
    }
    
    server {
        listen 8080;
        server_name _;
        
        # Health check
        location /health {
            return 200 'healthy\n';
            add_header Content-Type text/plain;
        }
        
        # WebSocket P2P Proxy (main feature for single-port P2P)
        location /p2p {
            proxy_pass http://ws_p2p_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 86400;
        }
        
        # RPC endpoints
        location /rpc/ {
            proxy_pass http://rpc_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket support for RPC subscriptions
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_read_timeout 86400;
        }
        
        # Direct RPC access
        location ~ ^/(status|health|abci_info|genesis|validators|block|blockchain|tx|tx_search|abci_query)$ {
            proxy_pass http://rpc_backend$uri$is_args$args;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # REST API endpoints
        location /api/ {
            proxy_pass http://api_backend/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # CORS headers
            add_header Access-Control-Allow-Origin *;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
            add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization";
            
            if ($request_method = 'OPTIONS') {
                return 204;
            }
        }
        
        # Direct Cosmos SDK REST access
        location ~ ^/(cosmos|ibc|speculod)/ {
            proxy_pass http://api_backend$uri$is_args$args;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # CORS headers
            add_header Access-Control-Allow-Origin *;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
            add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization";
            
            if ($request_method = 'OPTIONS') {
                return 204;
            }
        }
        
        # gRPC-Web proxy
        location /grpc/ {
            grpc_pass grpc://grpc_backend;
            grpc_set_header Host $host;
            grpc_set_header X-Real-IP $remote_addr;
            grpc_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            grpc_set_header X-Forwarded-Proto $scheme;
            
            # CORS for gRPC-Web
            add_header Access-Control-Allow-Origin *;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
            add_header Access-Control-Allow-Headers "Origin, Content-Type, Accept, Authorization, grpc-timeout";
            
            if ($request_method = 'OPTIONS') {
                return 204;
            }
        }
        
        # Default endpoint - Service information
        location / {
            return 200 '{
                "name": "Speculod Single-Port Node",
                "version": "1.0.0",
                "description": "Self-contained validator with nginx proxy and WebSocket P2P bridge",
                "endpoints": {
                    "rpc": {
                        "description": "Tendermint RPC endpoints",
                        "paths": ["/rpc/*", "/status", "/health", "/genesis", "/validators", "/block/*"],
                        "examples": ["/status", "/rpc/status", "/genesis"]
                    },
                    "api": {
                        "description": "Cosmos SDK REST API",
                        "paths": ["/api/*", "/cosmos/*", "/ibc/*", "/speculod/*"],
                        "examples": ["/cosmos/bank/v1beta1/supply", "/api/cosmos/bank/v1beta1/supply"]
                    },
                    "grpc": {
                        "description": "gRPC services",
                        "paths": ["/grpc/*"],
                        "note": "Use gRPC-Web client for browser access"
                    },
                    "p2p": {
                        "description": "WebSocket P2P bridge",
                        "path": "/p2p",
                        "note": "Connect using WebSocket for P2P communication"
                    },
                    "health": "/health"
                }
            }\n';
            add_header Content-Type application/json;
        }
    }
}
